
# .github (Org-wide GitHub Community Health Files)

Place this repository as `.github` in your GitHub org/user to apply default templates:
- Pull request template
- Issue templates
- Security policy pointer (no secrets)
